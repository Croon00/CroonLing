server:
  port: 8888  # Config Server 포트

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/YOUR_GITHUB_REPO/config-repo.git
          default-label: main
          clone-on-start: true